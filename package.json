{
  "name": "subtitle-blocker",
  "version": "1.0.0",
  "description": "A cross-browser WebExtension that blocks burned-in subtitles with a draggable overlay rectangle.",
  "private": true,
  "scripts": {
    "dev": "web-ext run",
    "dev:chromium": "web-ext run --target chromium",
    "lint": "web-ext lint",
    "build": "web-ext build --overwrite-dest"
  },
  "devDependencies": {
    "web-ext": "^8.3.0"
  }
}
